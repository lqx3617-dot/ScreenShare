{
  "changelog": "专属房间「进入等待」修复：观看方进入时若共享方尚未建房，不再被「会议号不存在」直接踢出，改为留在会议室自动喊 TA 并每 10 秒重试（最多 12 次）；服务端 room-status 查询与 create/join 分开限流（120 次/分），修复多设备共用出口 IP 时专属房间在线状态被 429 打断导致状态失真；客户端专属房间在线状态轮询间隔由 5 秒放宽到 10 秒",
  "forced": false,
  "minVersionCode": 0
}