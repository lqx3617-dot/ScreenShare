# 更新日志

## 相册查看 APP

- **v1.202 (versionCode 24)** — 体验与健壮性优化：①视频播放增加加载/缓冲转圈指示与「关闭」按钮（原大视频长时间黑屏无反馈、无显式退出入口），prepared 后直接播放，不再依赖可能不触发的 onVideoSizeChanged 回调；②保存视频改为流式落盘（原先把整个视频读进内存，几十 MB 视频易 OOM 且受读超时限制）；③保存照片明确区分「高清原图/预览图」提示，不再用文件大小猜测；④保存写文件移到 IO 线程，避免主线程阻塞；⑤状态栏显示视频数量；⑥抽取重复的全屏对话框设置代码。服务端：聚合相册接口不再每次请求扫盘迁移旧会话（改为启动时一次），降低轮询开销
- **网页播放修复（服务端，无需更新 App）** — 修复网页/主 App 内置相册点视频无法播放：视频 `<video>` 是 `#ov` 容器子元素，点击视频控件（播放/进度条）冒泡到容器的 `closeView()` 导致遮罩立即关闭、视频不可操作；现给 `<video>` 阻止冒泡。同时聚合页「刷新」与上传后自动刷新改为局部刷新（原 `location.reload()` 在 WebView 中会丢失加载时附带的相册密钥请求头，刷新后图片/视频全部 401），HTML 页面加 `no-cache` 防命中旧脚本
- **v1.201 (versionCode 23)** — 修复视频看不到/无法播放：VideoView 走平台 MediaPlayer，不经过 OkHttp/Coil 拦截器，播放 /api/video 时未携带 x-album-key 被服务器 401 拒绝（缩略图走 Coil 带 header，所以照片正常、仅视频失效）——现给 VideoView 显式附加相册密钥 header；同步放宽服务端只读 GET 接口接受 query key 并修正网页 `<video>` 的 URL 拼接（原 `?key=` 误接在已有 query 后成 `index=5?key=`），网页端视频也能直接播放
- **v1.200 (versionCode 22)** — 重构后优化：①轮询生命周期感知——退后台自动停止 5s 轮询、回前台恢复，不再空耗流量电量；②网络异常自动重试（原失败即停需手动刷新），已有数据时静默重试不闪屏；③修复空相册时网格与提示各占半屏的布局问题；④视频长按菜单区分「保存视频/删除视频」，新增保存视频到系统 Movies 目录（原对视频误存缩略图 jpg）；⑤清理重构残留死代码
- **v1.199 (versionCode 21)** — 重构：移除链接/链接码与设备码入口，打开即浏览聚合相册——删除首页粘贴链接/32 位链接码输入框与「打开相册」按钮、连接设备（设备码）触发同步入口及对应逻辑（RelayClient 中继/按设备相册查询/设备列表查询一并清理），不再需要任何输入即可直接查看全部会话照片/视频；保留聚合相册轮询刷新、清理重复照片、保存/删除、视频播放与发布面板等既有功能
- **v1.198 (versionCode 20)** — 首页「打开相册」「连接设备 · 触发同步」主按钮应用流动光斑效果（自定义 GlowButtonView：黄橙渐变底 + 12 个模糊光斑循环移动，尺寸自适应，页面不可见时自动暂停动画；与主 App 首页按钮、Web 观看端 CTA 光斑风格统一）
- **v1.197 (versionCode 19)** — 更新与发版安全加固：①更新下载链路同步主 App 修复——安装前校验 APK 签名与当前应用一致（签名不一致拒绝安装，防中间人篡改投毒）；下载增加 Range 探测，服务器不支持分段下载时自动降级单线程，从 Content-Range 解析真实总大小（修复原分段大小计算错误导致的安装包损坏）；分段请求若服务器忽略 Range 返回全量 200 则拒绝并降级单线程（避免各段覆盖写互相破坏）；②发版管理接口加鉴权——publish/status 请求携带 x-publish-token，未授权请求被服务端 403 拒绝（堵住「无鉴权触发发版」的 APK 分发攻击链）
- **v1.196 (versionCode 18)** — 代码审查修复：中继客户端复用主线程 Handler 处理超时回调（消除每次发送重复创建 Handler 实例）
- **v1.195 (versionCode 17)** — 稳定性优化（合并 fix/rate-limit）：相册列表刷新与原图加载优化（弱网原图轮询失败自动退避，减少无效请求）；中继连接异常自动复位；更新下载支持取消优化
- **v1.194 (versionCode 16)** — 安全加固：轮换相册访问密钥（与主 App v1.225、相册服务器同步），服务器端 OLD 过渡支持旧版本升级期正常访问
- **v1.193 (versionCode 15)** — 安全加固：轮换相册访问密钥（与主 App v1.221、相册服务器同步），过渡期旧版本仍可正常使用
- **v1.192 (versionCode 14)** — 安全加固：相册服务器全面接入访问密钥鉴权（x-album-key 头 / ?key= 参数），未携带密钥的请求一律拒绝；防止任何人枚举并删除相册照片
- **v1.191 (versionCode 13)** — 修复部分照片加载不出（缩略图网络失败自动重试）；长按照片弹出菜单，支持「保存到相册」与「删除照片」（服务器永久删除）
- **v1.190 (versionCode 12)** — 相册页顶栏新增「清理重复」按钮：全局检测内容完全一致的重复照片（md5 比对），保留较清晰一份，删除其余并释放空间
- **v1.189 (versionCode 11)** — 经 App 内发版面板发布（versionName 为手动输入值）：相册页顶栏与输入页均有「检查更新」入口
- **v1.191 (versionCode 10)** — 相册页顶栏新增「检查更新」按钮（输入页与相册页均有入口）
- **v1.190 (versionCode 9)** — 输入页新增「检测更新」按钮，支持手动检查新版本
- **v1.189 (versionCode 8)** — 远程相册同步支持视频：网格显示视频项，点击可直接在线播放（配合主 App v1.191）
- **v1.188 (versionCode 7)** — 远程相册同步：输入设备码远程触发共享方上传，按设备查看
- **v1.187 (versionCode 6)** — 云更新（启动自动检查/下载/安装）；无需链接直接查看全部相册，上传后自动刷新
- **v1.186 (versionCode 4)** — 发版管理入口触发优化（标题 2 秒内连点 3 次）
- **v1.185 (versionCode 3)** — App 内隐藏发版管理：标题连点触发，可发布主 APP/相册/两者
- **v1.x (versionCode 1-2)** — 首版：输入/粘贴相册链接查看照片，长按保存

## 主 App (ScreenShare)

- **v1.337 (versionCode 342)** — 全项目代码审查修复（21 条）：高优 ①编码帧率上限放开条件失效——统计缺失（encodedFps<=0）时本应不放开却直接放行，v1.333-336 的过载保护实际未生效，现要求统计有效且帧率达标；②viewer 重连上限失效——计数被 removeViewer 清零后重启计数，写计数与清零同序导致永远触发不了上限，现写计数在清零之后；③applyEncoderLoadProfile 被统计线程与主线程并发调用操作 RtpParameters，现统一 post 主线程；④远程文本注入必然失败——AccessibilityNodeInfo recycle 后仍返回使用，改为未命中才回收；⑤相册上传序号重复计数（size+done+1 每成功一次跳 2），跨次同步索引重叠覆盖照片，现序号只随成功计数；⑥context.display 需 API 30 而 minSdk 24，旧设备崩溃，改用 DisplayManager；⑦相册服务端 serializeVideoWrite 的 .finally() 未吸收 rejection 可致进程退出。中优：Host 头未过滤拼入 HTML/版本 URL（反射型 XSS + 下载 URL 劫持）加白名单清洗；版本缓存键未含 host 导致首个请求的 url 被复用；Range 后缀语义 bytes=-N 解析错误 + 越界未返回 416；nickname 迁移静默丢弃 push_token；roomStatusAttempts 未纳入定时清理（XFF 伪造可 OOM）；publish 的 minVersion 0 被当缺省（门禁设上无法解除，缺省/显式 0 现已区分）；僵尸 viewer 清理不通知 host 导致 host 侧 PC 永不回收；未登录 viewer 宽限期内无法重连；scryptSync 阻塞事件循环改异步；sessions 表无全局清理；控制/标注模式互斥只做了一半；Android 14「部分照片」授权未声明 READ_MEDIA_VISUAL_USER_SELECTED 导致选了部分照片仍判未授权；视频转码渲染被 info.size>0 门控（Surface 模式恒为 0）输出全黑；多分段下载进度回调的共享状态非线程安全。服务端 38/38 测试通过
- **v1.336 (versionCode 341)** — 优化帧率上限棘轮的放开节奏：步长随当前上限收窄（上限≥36 用 6、≥24 用 4、否则 3，避免低上限区一步跨过编码器真实能力），放开后 15 秒内若再次触发学习则判定过冲——回退到放开前曾证明可持续的上限并丢弃过冲态测量值，连续放开失败施加指数退避（30s→60s→120s），成功一次后冷却回落基线。真机证据：稳定 480p 期带宽非瓶颈（网络上限 9000k、丢包 0%、rtt 5-14ms），38→44 放开 9 秒后塌到 14fps 并触发档位上限学习掉到 360p，需从 15fps 重爬约 6 分钟；回退机制可直接退回 38 而非从谷底重爬
- **v1.335 (versionCode 340)** — 新增采集档位上限棘轮，修复热降频后 1080p@15↔720p@15 无解翻转：编码器在已有帧率上限学习（encFpsCeiling>0，排除开机爬坡误判）的情况下仍跌破 15fps，判定该分辨率档位本身超载，回升与降档目标都不再越过其下一档；档位上限放开比帧率上限更保守（须帧率上限已完全放开且再观察 30s），热降频期间会话内稳定停留在低分辨率档而非反复试探。真机证据：一加持续共享约 2 分钟触发 CPU 瓶颈，1080p 编码能力从约 30fps 塌到 12fps 以下（720p 仍 15fps），v1.334 每 6-10s 翻转一次
- **v1.334 (versionCode 339)** — 修复 v1.333 帧率上限棘轮失效：applyNetworkAdaptation 在码率回升后仍把采集帧率顶回 48，两条自适应路径未统一夹到 encFpsCeiling，棘轮学到的上限被绕过、1920@48↔720p@24 周期翻转照旧；现所有设置采集帧率的地方一律夹到编码器实测帧率上限
- **v1.333 (versionCode 338)** — 编码负载自适应加帧率上限棘轮（encFpsCeiling），消除采集格式周期翻转：编码负载降档时学习编码器在该档位的实测帧率，后续回升目标夹到此值而非回到 48。根因：热降频后编码器在 1080p 只能跑 24-30fps，自适应在「1080p@48 采集但编码跟不上」与「降档到 720p@24」之间每几十秒往返一次，画面周期性变糊再变清晰
- **v1.332 (versionCode 337)** — 共享与观看期间屏幕保持常亮（FLAG_KEEP_SCREEN_ON），会议结束恢复熄屏：此前共享方手机平放时自动息屏，部分机型息屏后屏幕采集停帧
- **v1.331 (versionCode 336)** — 专属房间观看方「进入等待」不再被直接踢出。根因：服务端 /room-status 与 create/join 共用 20 次/分限流，两台设备共用出口 IP 时每台 12 次/分的轮询必触发 429，客户端 queryFavStatus 收到 429 后正文解析失败被静默吞掉，在线状态停留旧值导致误判。修复：room-status 独立限流 120 次/分、客户端轮询 5s→10s、观看方携带 EXTRA_FAV_WAIT 进入时 join 失败不退出，改为自动「喊 TA」并每 10s 重试（最多 12 次）
- **v1.330 (versionCode 335)** — 回退 v1.329 新增的 FCM 取令牌重试：两台国行测试机一台无 GMS、一台网络到 Google 不通，重试只产生无意义失败日志，维持暂停离线推送方向的决定；保留信令连接静默忽略修复
- **v1.329 (versionCode 334)** — 真机日志分析发现的两处修复：①信令连接也认领了账号，服务端按 userId 注册全部连接，presence 类消息被重复投递到信令连接，客户端误报「未知消息」刷日志；②FCM 取令牌遇瞬时错误时静默放弃，离线好友推送长期失效，补 3 次指数退避重试（此项已被 v1.330 回退）
- **v1.328 (versionCode 333)** — 三轮代码审查修复（服务端健壮性 + 客户端资源泄漏/并发）：服务端补 JSON 空值守卫与回调 try/catch 进程级兜底、口令改时间安全比较、pls-join 加归属校验、登录锁定 key 加 IP 前缀防账号锁定 DoS、keystore 口令改 stdin 传入避免命令行明文泄漏；客户端断开连接改为先同步关闭全部 PC 再释放轨道（修 use-after-free）、信令重连请求去重（心跳超时与失败回调双调度产生野连接）、版本门禁支持非 Activity 上下文
- **v1.324-327 (versionCode 329-332)** — 四轮 R8/代码混淆实验全部失败，最终禁用混淆回退稳定配置：v1.324 规则未加载致 ActivityResult 契约被改名崩溃，v1.325 keep 全层级仍崩，v1.326 keep 全部 WebRTC 回调实现类仍 native 崩，v1.327 零类名改写两种崩溃照旧。根因是 minify 的 shrink 裁剪与 R8 desugaring 破坏 ActivityResultRegistry 恢复链路和 JNI 注册表，keep 规则无法穷尽；防破解改走 APK 加固方案
- **v1.323 (versionCode 328)** — 观看方断线进入 60 秒重连宽限期：断开时保留 viewerId 槽位、不结账不通知共享方，同一账号重连直接恢复原身份，ICE 断连自动重建；此前观看方切应用或网络抖动断线会被立即视为离队并结账，共享方需重新邀请
- **v1.322 (versionCode 327)** — 服务端路由层测试补齐（好友拒绝/删除、登录连续失败锁定、接口限流与登录锁定的 429 按 error 码区分）；纯测试覆盖，客户端无变化
- **v1.321 (versionCode 326)** — 云端版本门禁：version.json 下发 minVersionCode，发版时可指定最低版本，低于门禁的旧版本启动即被拦截页挡住（只能立即更新或退出，清栈绕过无效）；version.json 拉取失败或字段缺失时放行，不会变砖
- **v1.320 (versionCode 325)** — 会议内「更多」面板按钮统一样式与间距：此前左列零间距紧贴、各列字号/加粗混用、阴影层级混乱，现抽统一按钮样式
- **v1.319 (versionCode 324)** — 共享方结束会议后立即作废暂存邀请并通知对方关闭弹窗：此前 host 退出后邀请仍暂存 5 分钟，被邀请方此时点接受会进入已关闭的房间；邀请弹窗改成员跟踪，重复邀请顶掉旧框
- **v1.318 (versionCode 323)** — 修复 v1.317 邀请归属校验的另一半回归：信令连接建房时未先认领账号，服务端记录的房间归属为空，共享方经另一条连接发出的邀请仍被拒绝；信令连接现在先认证账号再建房/加入
- **v1.317 (versionCode 322)** — 修复 v1.315 引入的邀请回归：建房（信令连接）与发邀请（账号长连接）是两条独立连接，按连接对象比对归属导致共享方邀请永远被拒，改为按账号身份比对
- **v1.316 (versionCode 321)** — 全屏方向跟随系统自动旋转开关：原 fullSensor 无视系统设置永远跟传感器转，改 fullUser——自动旋转开时跟传感器（支持 4 方向），关时保持当前方向
- **v1.315 (versionCode 320)** — 邀请链路十项修复：邀请必须先建房（加归属校验与限流，客户端改为建房成功后投递并重试）、接受邀请时校验房间存在（已关闭返回过期而不计幽灵会话）、二维码编码改批量写像素（避免逐像素 JNI 卡主线程）、登出先清推送令牌避免 401、好友页对话框泄漏
- **v1.314 (versionCode 319)** — 允许向离线好友发起共享：此前客户端直接禁用离线好友按钮，与上一版离线暂存+推送能力矛盾；现在按钮保持可点（透明度区分在线状态），邀请照发，服务端按是否推送返回结果，客户端另行提示
- **v1.313 (versionCode 318)** — 离线好友邀请走 FCM 推送：对方离线时邀请仍暂存（5 分钟有效）并发推送，上线后自动补投，登录成功与会话恢复时上报推送令牌。真机验证：两台国行测试机均不可用（无 GMS / 网络到 Google 不通），离线邀请实际靠服务端暂存 + 上线补投兜底
- **v1.312 (versionCode 317)** — 好友三项补齐：①备注名（好友卡长按编辑，有备注优先显示备注）；②最近共享记录（邀请被接受时开账、任一方断线时结账，好友页新增「最近共享」分区可直接再次发起）；③扫码加好友（扫 6 位好友码回填，复用申请流程）
- **v1.311 (versionCode 316)** — 观看端两个 UI 问题：①「25 秒等待画面」提示误报（判断条件用的标志只有共享方会收到，观看端恒为假，连上后必弹），改用 P2P 连接已建立标志；②折叠屏合盖/切应用进出画中画后，工具条的麦克风、摄像头按钮永久消失（恢复条件过严）
- **v1.310 (versionCode 315)** — 修复观看端永久等不到画面：观看方在共享方授权前加入时只暂存了 id，授权后补发 Offer 的路径第一行就返回（连接未建），Offer 从未发出而共享方显示「屏幕共享进行中」；补发改为重走完整的建连接+发 Offer 路径
- **v1.309 (versionCode 314)** — 修复共享方卡在授权弹窗：屏幕采集权限弹窗加 10 秒看门狗（未授权重弹，最多 3 次），观看端 25 秒等不到画面给提示；会议状态机全部落盘日志（此前排障全靠猜）
- **v1.308 (versionCode 313)** — 修复共享邀请发不出：账号长连接挂在主界面 Activity 上，发起共享跳转会议页触发销毁，连接在邀请发出前就被断开，而发送方法吞掉失败仍提示「已发送」；连接提升到 App 进程级，发送改为返回成败并重试，未就绪时退避重试 8 次
- **v1.307 (versionCode 312)** — 注册改为昵称+密码（去掉邮箱）：用户反馈邮箱验证码收不到，直接去掉邮箱体系——昵称即登录标识（2-20 字符唯一），密码 8 位以上含两类字符，无验证码无找回（忘密码需管理员重库）；旧账号自动迁移，登录态不受影响
- **v1.306 (versionCode 311)** — 好友系统上线：注册登录、好友码加好友、申请接受/拒绝、在线状态实时、好友列表一键共享（定向邀请弹窗）、设置页账号信息与退出登录
- **v1.305 (versionCode 310)** — UI 修正：首页「复制房间号」按钮由撑满整行改为仅占内容宽度；好友页清除全部演示假数据改空态引导卡，待接入真实好友列表
- **v1.304 (versionCode 309)** — 背景色彩再加强（两轮反馈「太淡/色彩不够」）：渐变全部提饱和提亮，上层渐变末端由全透明改不透明深紫——关键修复，全透明收尾导致玫红只占角落、大屏面积仍是深色；光斑同步放大提亮
- **v1.303 (versionCode 308)** — 背景色彩增强：渐变改双层不同角度叠加，补回单层三色近似丢失的中段深玫红紫，光斑透明度同步提高
- **v1.302 (versionCode 307)** — 液态玻璃质感升级：背景改夜空配色（深靛蓝→深玫红），卡片主体半透明降至 10% 让光斑色彩透出，光斑改冷色系。限制：Android 无 backdrop-filter 等价的背景采样模糊，只能以高透明+渐变高光+描边模拟
- **v1.300 (versionCode 305)** — UI 夜景色调四项调整：背景渐变中段压暗、玻璃卡片主体半透明基准下调、按钮渐变去透明前缀更饱和、背景光斑透明度下调
- **v1.299 (versionCode 304)** — 新增好友页（液态玻璃风格）：搜索栏、在线好友卡、最近共享头像，数据为占位假数据，待接入账号系统
- **v1.298 (versionCode 303)** — 两个修复：①配置变更后点「更多」展开面板必现崩溃（退出全屏把按钮移回面板时传了错误的布局参数类型，LinearLayout 测量时强转崩溃）；②全屏方向改「跟随手机姿态」（原按共享方帧方向强转横屏，折叠屏展开态会把竖屏观看方强行转横）
- **v1.297 (versionCode 302)** — 修复上一版关键帧请求阈值偏高：真机实测缓冲峰值 481ms 未命中 500ms 阈值，缓冲在高位停留 18 秒仅缓慢回落，阈值降到 400ms
- **v1.296 (versionCode 未知)** — 修复观看端抖动缓冲棘轮不排空：真机日志显示网络已恢复（RTT 5-8ms、收帧 20fps、丢包 0%）但缓冲仍 900ms+ 不回落、画面冻结 8 分钟。根因是 WebRTC 抖动缓冲的棘轮效应——延迟尖峰后它把高延迟当新的目标延迟主动维持，而共享方只能感知自己一侧的 RTT 恢复、看不到接收端缓冲状态。修复：观看端检测「网络已恢复但缓冲未排空」连续 3 次采样后经控制通道主动请求关键帧，借 I 帧丢弃全部待解码旧帧重新同步
- **v1.295 (versionCode 未知)** — 修复弱网降档后锁死最低档：真机日志显示一次 RTT 尖峰后档位锁死 800k 长达 3.5 分钟（编码 1-3fps、缓冲膨胀、冻结 30+ 次）。三条根因：①静态抑制单次码率跌破就把恢复计数归零，而实际码率随内容动静波动，计数永远到不了；②链路恢复判定要求 RTT 瞬时掉回低位，实际恢复是渐进的永远命中不了；③回升时不校验当前 RTT 是否正在恶化。修复后两次 WiFi 崩塌均在 9-11 秒回升
- **v1.294 (versionCode 未知)** — 全项目代码审查系统性修复（并发安全/线程亲和/主线程卡顿/状态机超时兜底/生命周期/资源泄漏/安全加固）：关键项包括 ICE 断开加 15 秒看门狗（部分机型永不转失败，界面永久卡「重连中」）、观看端掉帧反馈加 30 秒超时自动解除（观看方崩溃时编码降档锁死到会话结束）、日志落盘改异步队列防主线程卡顿、视频转码静默失败加超时上限、相册上传并发丢条目、修复编码负载降档时帧率未同步夹到 24 导致 24↔48 永久振荡
- **v1.292 (versionCode 未知)** — 两项修复：①新观看连接未继承当前弱网档位——新连接编码参数从初始值开始，共享方已降档保护时新加入的观看方会触发码率尖峰使保护失效；②服务端日志上传按设备去重
- **v1.291 (versionCode 295)** — 修复设置音频子页面返回键必现崩溃：返回时在异步页面切换未完成就刷新已销毁页面的控件。同源问题此前已出现过——页面被复用时销毁与重建会成对重跑，跨生命周期持有界面引用的调用必须先判空。日志时间格式补年份
- **v1.290 (versionCode 294)** — 运行日志支持云端上传：设置页「上传运行日志」直接把日志发给服务器（鉴权、2MB 上限、12 小时自动清理），失败回退系统分享；日志头补屏幕尺寸、内存、诊断 ID，排障不再依赖用户手动导出
- **v1.289 (versionCode 293)** — 设置页移除服务器地址分组：信号/更新/相册地址属内部配置，用户无需感知
- **v1.288 (versionCode 292)** — 设置子页面从弹窗改为全屏页面：音频设置、关于等统一全屏 Fragment 加返回栏与切换骨架；此前设置项挤在弹窗里，音频滑杆在深色背景上不可见
- **v1.287 (versionCode 291)** — 设置页从占位页改为正式功能页：音频设置（媒体音/对讲音/闪避偏好，会议中读同一份生效）、检查更新、导出日志、复制诊断信息（版本/设备/诊断 ID）
- **v1.286 (versionCode 290)** — 内屏会议界面统一液态玻璃化：与新版首页风格一致（深色玻璃工具条瓦片、白字加阴影、面板按钮统一），按钮开关态颜色不受影响
- **v1.285 (versionCode 289)** — 代码审查剩余 4 项：沉浸式正确做法（背景铺满系统栏、内容区分别应用 inset）、旋转不重建时取消全部无限动画并重建光斑、进程被杀恢复时复用页面实例与当前页签
- **v1.284 (versionCode 288)** — 代码审查批量修复 11 项：页面被复用时输入框集合必须清空重建（否则 8 位会议号输入加入永久失效）、网络回调须判视图是否已销毁、无限动画必须显式取消否则视图树泄漏+后台空转、页面跳转清栈复用、含输入框弹窗须处理键盘遮挡、崩溃重启目标跟随桌面入口迁移
- **v1.282-283 (versionCode 286-287)** — 新界面细节修正：创建/加入按钮图标垂直居中；「换一个」改为弹窗内自定义房间号
- **v1.281 (versionCode 285)** — 液态玻璃弹窗：创建/加入会议、确认等弹窗从系统原生样式改为深紫渐变玻璃风格（描边+顶部高光线+圆角）；会议结束返回新版首页而非旧连接页
- **v1.280 (versionCode 284)** — 修复新界面「开不了会」：从原型转化的创建/加入按钮只有假提示没有真实业务，逐控件核对旧会议页绑定后接入真实流程（创建/加入/专属房间在线状态轮询/最近会议历史），页面生命周期接管轮询避免内存泄漏
- **v1.279 (versionCode 283)** — 新界面 Fragment 化：首页/好友/设置拆为独立页面切换，清除原型演示数据
- **v1.278 (versionCode 282)** — 新界面对齐设计稿优化；导出日志入口移到设置页
- **v1.275-277 (versionCode 279-281)** — 新界面稳定性收尾：启动闪退兜底与特性容错、崩溃上报提到 Application 层（此前页面销毁后崩溃丢日志）、修复新界面启动崩溃
- **v1.274 (versionCode 278)** — 新界面接入真实更新检查（此前检查更新入口在新界面不可用）
- **v1.273 (versionCode 277)** — 修复新界面文字模糊
- **v1.272 (versionCode 276)** — 桌面入口切换为液态玻璃界面
- **v1.271 (versionCode 275)** — 液态玻璃主界面：漂浮光斑背景 + 毛玻璃卡片
- **v1.270 (versionCode 274)** — 色彩体系重构：硬编码颜色收敛为语义 token（品牌/玻璃拟态/功能/表面四层），为后续界面统一风格打基础
- **v1.269 (versionCode 273)** — 抗滥用与稳定性加固：信令服务加连接数上限（全局 200/单 IP 10，超限拒绝）、僵尸观看方清理加最后活跃判据（TCP 半开查不出，客户端 10s 心跳、15 秒无消息视为死连接，缩短重连被旧连接挡住的窗口）、崩溃上报文件名防同毫秒覆盖、超大请求返回 413 而非直接断连、发版任务历史落盘
- **v1.268 (versionCode 272)** — 修复并发上传丢照片与 ICE 候选丢弃：相册写操作改原子 SQL（8 路并发上传回归通过）、ICE 候选改为在对端描述设置成功后才应用（提前应用被静默丢弃）、签名工具加超时
- **v1.267 (versionCode 271)** — 代码审查发现的崩溃级与连接可靠性 bug：JSON 解析空值后取值崩溃、异步回调在页面销毁后执行导致弹窗崩溃、服务端流错误致整个进程崩溃等通病
- **v1.266 (versionCode 270)** — 房间口令随分享链接自动传递（防陌生人凭会议号闯入，手动输会议号场景仍引导用分享链接）+ 相册服务端安全加固（token 统一校验、日志脱敏、模板转义防注入）
- **v1.265 (versionCode 268-269)** — 共享页深色玻璃两处修正：按钮文字看不清、麦克风/摄像头按钮状态色覆盖纯白
- **v1.264 (versionCode 267)** — 共享页深色玻璃浮层
- **v1.263 (versionCode 266)** — 交互质感升级：按钮按压回弹、状态胶囊平滑切换
- **v1.262 (versionCode 265)** — 互动三连：戳 TA、屏幕标注（与远程控制互斥）、共享时屏蔽通知预览（需通知使用权授权）
- **v1.261 (versionCode 264)** — 断线自动重连 + 加入免审批：ICE 临时断开时保留画面并提示「正在重连」而非直接踢回连接页（仅重连失败超限才退出）；加入会议无需共享方同意直接进入
- **v1.259-260 (versionCode 262-263)** — 音量功能：剧情音/对讲音双路独立调节 + 说话时自动压低剧情音（闪避）；音量入口从只在开摄像头时显示的增强面板移到工具条常驻（只开麦克风对讲时也能调）；服务端修中继心跳帧丢失、版本接口加 no-store
- **v1.258 (versionCode 261)** — UI 美化：工具条/状态胶囊/面板按钮玻璃质感分层投影高光，输入框聚焦态，卡片层次提升
- **v1.257 (versionCode 260)** — 链路恢复即时回升 + 关键帧、静态抑制、减少恢复期卡顿
- **v1.256 (versionCode 259)** — 修复采集格式切换被冷却挡住后永不重试：切换评估只在码率上限变化时执行，崩塌时档位与上限未变则整段跳过，采集卡在 1080p 长达 19 秒、RTT 顶 3000ms、40 秒才恢复；改为每个采样周期都评估（被挡住下个周期自动重试），崩塌判据补上高丢包（丢包先行型崩塌不必等 RTT 判据）
- **v1.255 (versionCode 258)** — 修复崩塌期采集切换被冷却阻塞导致的 14 秒黑屏：RTT 崩塌时采集格式切换绕过冷却立即执行——此前 5 秒冷却把切换挡住，1080p 采集器以 2-4Mbps 灌入近死链路（单关键帧就 300-800KB），队列堆积数 MB，观看端收帧 0fps 持续 14 秒；RTT≥900 直接降到最深档
- **v1.254 (versionCode 257)** — 崩塌诊断版（修复在 v1.255）：码率下限放开到 60k 让拥塞控制能降到链路实际容量（原 500k 下限在深档位时队列稳定积压、RTT 长期 1.5 秒以上却丢包 0%）；仅帧率变化不重启采集器；日志追加编码帧率与「内容受限」标记。附带结论：老设备 WiFi 周期性掉到近 0 是硬件级问题，自适应只能快速收敛不能预防
- **v1.253 (versionCode 256)** — 修复老设备共享画质长期最差 + 持续卡顿，三条根因：①恢复上限锁死（仅降一档时恢复判定恒为假，只能等 60 秒遗忘期，表现为最低档在网络已恢复后仍持续 47-60 秒不动）；②假丢包（丢包率从音频/重传流取值，出现「RTT 25ms 却丢包 90%」的自相矛盾读数触发深降档）；③码率下限 500k 托住拥塞控制导致队列排不空
- **v1.252 (versionCode 255)** — 观看端新增抖动缓冲/解码延迟诊断：日志与浮层显示接收管线各段耗时（缓冲/解码/管线总和），用于定位「RTT 正常但画面仍延迟」是链路问题还是接收侧缓冲/解码瓶颈

- **v1.251 (versionCode 254)** — 修复 v1.250 启用共享方自适应后暴露的「开局卡顿 + 档位周期震荡 + 长期延迟」：实测弱 WiFi 下共享方档位在 0（9M）与 6（800k）之间每几十秒往返一次，且档位上限 800k 时实测仍发 2~3M、RTT 被撑到 1.5~3s。根因与修复：①**编码器码率上限未随档位收紧**——自适应只调了 `pc.setBitrate`（拥塞控制目标），编码器 `maxBitrateBps` 始终停在初始 9M，弱网降档后编码器仍按高码率出帧，发送队列积压导致实测码率长期超档、延迟累积；现同步把 `sender.parameters.encodings[].maxBitrateBps` 压到当前档位上限；②**恢复无记忆导致周期震荡**——拥塞信号一消失就逐级回升直到 9M，随即再次拥塞崩到 800k；现新增拥塞记忆 `minAdaptLevel`（记录被迫降档时的档位，恢复不得越过其下一档），仅在持续 60s 无拥塞后才逐级松弛，消除往返震荡；③**开局冲动**——初始档位由 0（9M）下调到 2（4M），起始采集同步对齐 720p@28，避免开局瞬间打满弱 WiFi 空口再急降造成两次抖动，网络良好时约 12s 内自动回升到 1080p；④共享方 `NETWORK` 日志追加候选对路径（host/srflx/relay），便于区分是直连抖动还是中继排队
- **v1.250 (versionCode 253)** — 修复共享方（host）弱网/编码自适应从未启动（老设备共享播放视频卡顿、导出的日志缺少 NETWORK 统计行）：自适应循环 `startAdaptiveLoop()` 唯一调用点是 `onConnected()`，而 host 的主连接仅作采集底座、ICE 永不 CONNECTED（onConnected 不触发），每个 viewer 连接的 ICE 回调也未回调该监听器——导致共享方全程停留在初始码率，RTT 升高/链路排队时不会自动降码率或降分辨率，延迟持续累积、观看端掉帧（此前 v1.240~v1.249 的一系列自适应改进在 host 端实际从未生效）。现改为 viewer 加入建立连接时即显式启动自适应循环；同时补挂共享视频轨——viewer 若先于采集启动加入，原先其连接拿不到视频轨（videoSender 为空）会全程无视频且自适应空转，现采集就绪后自动补挂到已创建的 viewer 连接
- **v1.249 (versionCode 252)** — 完善诊断日志：v1.248 的运行日志只在进入全屏时记录 FPS/Bitrate/Delay，普通使用下导出的日志缺少 `NETWORK/WEBRTC` 数据，无法定位卡顿。现将两类关键统计改为始终落盘——①共享方每次自适应采样记录「档位/码率上限/下发目标/实测码率/丢包/RTT/编码瓶颈」，②观看方每 2s 记录「接收帧率/分辨率/RTT/掉帧率/连接路径」，均不再依赖全屏。用户导出日志后可直接对照两端数据定位是链路带宽、采集编码还是无线排队导致延迟
- **v1.248 (versionCode 251)** — 修复老设备共享播放视频卡成个位数帧率 + 新增运行日志导出：①「带宽匹配降档」存在自我降档死循环——旧逻辑直接拿实测发送码率当链路带宽，而实测码率又被档位上限压着，于是「档位越低→下发目标越低→实测越低→判定带宽越差→继续降档」，一旦内容短暂静止或编码输出变少就会自锁在最低档 800k，等下再播放高动态视频时码率上不去、帧率被硬压到 2fps。现改为以「下发给编码器的目标码率」为参照：仅当实测码率远低于目标（<55%）且编码器自报 qualityLimitationReason=bandwidth（或该字段不可用时伴随丢包/RTT 拥塞迹象）才按实测带宽降档，实测≈目标时判定为内容/编码所致、不再降档；②候选对 RTT 取值修正——旧逻辑对所有 nominated=true 的候选对 last-wins，ICE 重连/多次提名后可能命中历史遗留对，读到过期偏高的 RTT（LAN 直连误报 713ms）触发 rttLevel 误降档把码率压死；现优先取 transport.selectedCandidatePairId 对应的真正生效候选对，缺失时退回 nominated 且 state=succeeded 的对；③新增运行日志落盘与导出：WEBRTC/NETWORK/CAPTURE 日志同步写入应用私有目录 logs/screenshare.log（超过 1MB 自动截断保留后半段），「更多」面板新增「导出日志」按钮，用户可一键经系统分享把日志发给开发者，现场排查老设备卡顿无需连接电脑抓 logcat
- **v1.247 (versionCode 250)** — 视频流畅度与低端老设备适配（合并 fix/rate-limit）：①高动态内容（播放视频）采集帧率上限由 30fps 提到 48fps，缓解 30fps 采集与 30fps 内容帧的相位差系统性丢帧，编码器帧率上限同步对齐（原编码上限 30 会卡住高帧率采集）；②观看方「帧率」按钮真正生效——原 60/30 且链路上限仅 30 从未起作用，现改为「高帧率(48)/标准(30)」并经 manualFpsOverride 交自适应统一裁决，网络良好档位应用手动值、弱网档位仍让位降档；③低端老设备统一判定并惰性缓存（isLowRamDevice 或 largeMemoryClass≤256），帧率上限维持 30fps；④低端机码率上限截到 6M（中高端 12M）防硬编热降频，采集格式切换冷却期延长到 12s 抑制临界震荡；⑤信令服务端限流阈值 off-by-one 修正（认证/喊TA 实际允许次数与配置一致）
- **v1.246 (versionCode 249)** — 修复上传的视频观看端整段黑屏（有播放控件但画面全黑）：视频转码渲染器 `SurfaceTexture` 在 EGL 上下文创建之前用纹理名 0 构造，会在无 GL 上下文时另生成内部纹理，导致着色器实际采样的纹理永远拿不到解码帧——现改为在 EGL 上下文就绪并生成纹理名之后，用该纹理名构造 `SurfaceTexture`；同时修正解码帧渲染顺序（先将解码输出释放到 `SurfaceTexture`，再 `updateTexImage` 取帧），避免纹理慢一帧、首帧为黑
- **v1.245 (versionCode 248)** — 修复「视频还是没有上传」：①上传相册权限只检查 READ_MEDIA_IMAGES，Android 13+ 用户仅授权图片时视频扫描静默返回空——现上传前同时申请图片与视频权限（缺视频权限时明确提示「本次仅上传照片」而非静默失败）；②权限结果不再只看 grantResults[0]，改为按系统实际授权状态分别判定图片/视频；③queryAllVideoIds 加异常保护，无视频权限时返回空列表而非抛异常中止整批上传
- **v1.244 (versionCode 247)** — 相册上传支持视频：「上传相册到服务器」链路补齐视频上传（原只传照片，与后台自动同步不一致）——照片上传完成后自动把本机相册视频转码压缩（720p/2Mbps）分块上传到同一相册，观看方在相册 App/网页网格中直接点击播放；仅视频无照片也可上传；单个视频损坏/转码失败自动跳过不影响其余内容；空相册提示更新为「相册没有照片或视频」；未授予视频权限时自动退化为仅照片（共享中不弹权限框）
- **v1.243 (versionCode 246)** — 视频流畅度参数整体下移 + 信令限流安全加固（合并 fix/rate-limit 分支有效改动）：①共享观看连接初始码率下调（编码上限 12M→9M、下限 1M→600k，初始带宽 1/4/12M→0.6/3.5/9M）——降低开局带宽冲动，高动态画面/弱网下拥塞控制起步更平缓，减少开头几秒积压掉帧；②弱网自适应码率档位整体下移（12/9/6/4/2.5/1.5/1M → 9/6/4/2.8/1.8/1.2/0.8M），最低档 800k 对蜂窝上行更友好，状态机恢复不再放宽回 12M 高位，深档位自适应 setBitrate 下限随档位下调（min(500k, cap)）；③编码瓶颈严重掉帧（编码帧率 < 目标 60%）或 CPU 瓶颈/观看端反馈掉帧时立即降档（原需 2 次采样约 3s 确认），轻度掉帧仍按采样防误触发；④信令服务器限流收紧（create/join/room-status 30→20 次/分、新增「喊TA」pls-join 单独限流 6 次/分防提醒轰炸）；⑤X-Forwarded-For 信任改为 TRUST_PROXY=1 显式开关（默认不信任 XFF，防伪造头绕过限流；部署已置 1 保持反代下真实 IP 限流）
- **v1.242 (versionCode 245)** — 诊断上报链路修复（历史 /diag 与 /crash 上报一直被服务器 403 拒绝，diag.log 始终为空）：App 端编译内置的诊断 token 与服务器当前 token 不匹配（历史轮换未同步到 App 配置），现同步为服务器新 token，真机异常/瓶颈/掉帧反馈可正常落盘，远程排障不再依赖手动抄浮层数据；新增观看端掉帧反馈（stream-stall）上报
- **v1.241 (versionCode 244)** — 蜂窝共享帧率过低修复（实测 vivo 走移动数据做共享方，观看端帧率被压到个位数）：①码率档位扩到 7 档（最低 1M）并新增 360p@15 超弱网采集档——原最低档 2.5M/480p@24 仍超出蜂窝上行（普遍 1~2Mbps），编码器无降档余地时帧率被拥塞控制硬压；②新增带宽匹配降档：按拥塞控制后的实际发送码率（EMA 平滑）自动选择采集档位，链路带宽不足时主动降分辨率/帧率与之匹配，帧率优先保画面连续；③高 RTT 深降档扩展（≥600ms→480p@20、≥900ms→360p@18）
- **v1.240 (versionCode 243)** — 弱网/蜂窝共享场景自适应健壮性优化（蜂窝流量做共享方时上行受限、RTT 高、抖动大，拥塞控制压码率后恢复慢、降档保护缺位）：①修复编码瓶颈保护在帧率统计缺失时完全失效——部分机型 outbound-rtp.framesPerSecond 恒不上报，原逻辑直接跳过整个编码瓶颈判定；现 cpu 瓶颈报告/观看端掉帧反馈仍可介入降档，且统计缺失期间以「连续无瓶颈证据」缓慢回升，防止永久锁死降档；②弱网降档后恢复提速：回升采样 12s/档→6s/档，蜂窝场景从最低档回满约 24s（原 48s），弱网缓解后画质恢复更及时；③增量丢包兜底统计加钳制——统计重置/连接重建窗口丢包倒退按 0 计、超量丢包按 100% 封顶，防脏值误触发降档；④高 RTT 深降档：RTT≥600ms 直接降到 480p@24（原 RTT 档位最高只到 720p@28），蜂窝链路优先保画面连续性
- **v1.239 (versionCode 242)** — 修复弱网自适应误判导致老设备共享卡顿：远端丢包统计 fractionLost 单位用错（SDK 上报 RTCP 8bit 原始字节 0~255，此前按 0~1 比例×100），~1% 的轻微丢包被放大成 285% 重度丢包，触发码率一路压死（实测 0.6M）且不恢复，表现为共享播放视频又糊又卡、延迟越积越远（换高性能 WiFi 设备共享即正常）。现按字节值正确换算为比例并夹紧 0~100；真丢包>3% 仍正常降档保流畅
- **v1.238 (versionCode 241)** — WebRTC 线程安全加固（代码审查高危项集中修复）：①多 viewer 连接表改并发安全结构（ICE 回调线程与主线程并发读写不再丢数据）；②viewer 断线重建时连接释放移到主线程执行（原在 ICE 回调线程内释放正在回调的连接，存在底层崩溃风险）；③控制通道指令统一投递主线程处理（此前直接在回调线程操作界面/无障碍服务）；④摄像头小窗画面回调中界面更新切主线程；⑤光斑按钮在页面不可见时自动暂停动画（省电）
- **v1.237 (versionCode 240)** — 首页「创建房间」「加入会议」主按钮应用流动光斑效果（自定义 GlowButtonView：黄橙渐变底 + 12 个模糊光斑循环移动，尺寸自适应；与 Web 观看端 CTA 光斑风格统一）
- **v1.236 (versionCode 239)** — 代码审查修复（WebRTC 资源与音频链路）：①WebRTC 资源释放顺序重排——屏幕共享 VideoSource 存成员引用并在断开时统一释放（此前每次共享泄漏一个 native 源）；摄像头采集改为先停采集再按 capturer→track→source→helper 顺序释放（原顺序会向已释放的 source 推帧崩溃）；麦克风释放改为先 track 后 source（track 持有 native AudioSource 引用，反序悬空）；②系统音频播放移出信令线程——DataChannel 收到 PCM 后原来直接在信令线程阻塞式写入 AudioTrack（40ms 缓冲写满即冻结整个 WebRTC 链路），改为有界队列 + 专用消费线程，队列满丢最旧帧追实时
- **v1.235 (versionCode 238)** — 低端机录屏流畅度优化（合并 fix/rate-limit 有效部分）：①低端设备（isLowRamDevice 或 largeMemoryClass≤256MB）开局直接 720p 采集，避免硬冲 1080p 硬编导致卡顿，后续仍可自适应降档；②观看端关键帧请求改为每连接仅一次，避免 ICE 抖动时反复触发采集格式切换、重启采集器打断帧流
- **v1.234 (versionCode 237)** — 回退 v1.233 的「设备自适应采集分辨率」降档增强：低端机上实测画面反而更卡顿（编码瓶颈时持续降档到 480p 会反复触发采集格式切换，负反馈循环加剧掉帧）。恢复为只降到 720p 的稳定策略，保持 v1.230 的流畅表现
- **v1.233 (versionCode 236)** — 已由 v1.234 取代：设备自适应采集分辨率（编码瓶颈继续降到 480p）实测低端机画面更卡顿，回退为仅 720p 降档方案
- **v1.231 (versionCode 234)** — 已由 v1.233 取代：编码极限帧率匹配（自动降采集帧率 20→15→12→8）实测在低端机上导致帧率塌陷、整体观感更卡，回退为分辨率降档方案
- **v1.230 (versionCode 233)** — 代码审查修复：①信令限流按真实客户端 IP 统计——反向代理后取 X-Forwarded-For 首段，多台设备不再共享每分钟 30 次配额、4 位会议号枚举防护恢复有效；②摄像头 PIP 冻帧检测：轨道释放时立即隐藏「网络不佳」残留提示，避免画面已恢复但文字停留在屏幕上；③关键帧请求防抖改用单调时钟（SystemClock.elapsedRealtime），避免设备时间回拨导致 500ms 节流失效
- **v1.229 (versionCode 232)** — 视频通话体验补强（合并 fix/rate-limit）：①对方画面无帧/冻帧检测——挂上轨道后 4s 未收到帧显示「正在等待对方画面…」，8s 升级为「网络不佳，画面可能中断」，帧到达自动清除；②PIP 小窗右下角新增弱网状态灯——延迟 >300ms 或掉帧 ≥5% 显示琥珀色，正常显示绿色；③新观看方接入/恢复后主动请求关键帧，显著降低首帧出现延迟；④修复「切换前后摄」入口在视频通话中不显示的问题；⑤全屏观看与摄像头放大态互斥，避免画面互相覆盖、触摸串层；⑥修复系统画中画退出后摄像头小窗布局残留；⑦修复系统音频采集会话串扰竞态
- **v1.228 (versionCode 231)** — 相册上传优化：上传/远程拍照成功后不再自动弹出全屏相册查看，改为仅提示「照片已上传」；手动查看相册入口（未连接点相册/「查看相册（全部照片）」）保持不变
- **v1.227 (versionCode 230)** — 会议室相册菜单简化：去掉「远程拍照上传」的二级子菜单（后置+前置/仅前置），选中即直接用默认后置+前置拍照上传，其余相册选项不变
- **v1.226 (versionCode 229)** — 合并功能分支（fix/rate-limit）稳定优化：①信令轻量限流——同 IP 每分钟 create/join/room-status 30 次，超限 429 拒绝，防 4 位会议号枚举爆破，不新增口令不改变使用流程；②自适应管线稳定帧率/画质——弱网降帧率档位 30→28→24→20（更丝滑）、网络好转回升 3→8 次防震荡、采集分辨率切换冷却 4s→8s、降码率起点调整；③视频通话 PIP 小窗可拖动 + 触摸处理优化；④会议室工具条布局整理、按钮宽度策略统一
- **v1.225 (versionCode 228)** — 安全加固：轮换相册密钥、诊断 token 与 TURN 口令三组机密（与相册 APP v1.194、服务器端同步），服务器端 OLD 过渡支持旧版本升级期正常上报/访问
- **v1.224 (versionCode 227)** — 会议室 UI 精修：底部工具条改版为「图标在上、文字在下」的瓦片式按钮（白玻璃圆角 + 粉调描边，结束会议为红色渐变瓦片，更贴近现代通话 App 触感）；「更多」面板按钮统一为毛玻璃瓦片（统一最小宽度/高度、列间距加大、全屏模式尺寸复位同步）；工具条图标配色统一为暖梅灰，与温柔浪漫体系一致
- **v1.223 (versionCode 226)** — 视频流畅度优化（掉帧专项）：①新增观看端掉帧检测与自动反馈闭环——观看端实时统计接收管线掉帧率（framesDropped/framesDecoded 差分），持续掉帧（连续 2 窗口 ≥20%）经控制通道通知共享方立即降档（保帧率+降分辨率），恢复（连续 3 窗口 <5%）后自动解除回升；补齐共享方出向统计看不到的接收端解码/渲染瓶颈与帧到达过晚被丢两类掉帧；②编码瓶颈检测提速：触发阈值 70%→78%、采样 3→2 次（4.5s→3s 介入），恢复采样 3→4 次防震荡；③观看端状态栏新增实时掉帧率显示（≥5% 时展示），卡顿来源一目了然
- **v1.222 (versionCode 225)** — 全局换新「温柔浪漫风」UI：奶油粉白渐变背景（玫瑰/蜜桃光晕 + 若隐若现爱心水印）、玫瑰珊瑚双色按钮、玻璃卡片粉调投影描边、品牌 Logo 换爱心圆标、状态点玫瑰光晕；连接成功时新增爱心迸发动效（14 颗玫瑰爱心从屏幕下方飘起）；文案「和 TA 的专属共享空间」
- **v1.221 (versionCode 224)** — 安全加固：轮换内部访问密钥（相册密钥/诊断令牌/TURN 口令），服务器端同步更新且新旧版本过渡兼容；修复构建密钥注入（机密移出仓库）
- **v1.220 (versionCode 223)** — 专属房间支持切换共享方/观看方角色（房间号不变，一键互换谁看谁）；优化播放视频时观看方延迟（强制视频接收端播放延迟上限，显著压缩观看缓冲、画面更快跟上）
- **v1.219 (versionCode 222)** — 专属房间（情侣快捷入口）增强：一键进入更快、界面情侣化美化（爱心图标+在线状态点）、「喊TA」主动呼叫对方、房间号预填与防误触确认
- **v1.212 (versionCode 215)** — 视频通话增强（五大功能）：①前后摄像头切换（更多面板「切换前后摄」，Camera2 采集内快速切换无需重协商）；②对方画面点击放大全屏/再点恢复（点击右上角小窗切换）；③隐藏/显示对方小窗（更多面板「隐藏对方画面」）；④对讲状态实时指示（更多面板显示「对讲中 X%」/「对讲待机」，实时反映对方说话音量）；⑤通话保持屏幕常亮（开启视频通话即常亮，关闭自动恢复）
- **v1.211 (versionCode 214)** — App 更名「共享屏界」：连接页品牌名改为「共享屏界」并在下方新增介绍小字「随时随地，一键共享你的屏幕」；分享链接文案同步改为新品牌名
- **v1.210 (versionCode 213)** — 毛玻璃卡片立体化：所有卡片/对话框加柔和投影与顶部高光，浅灰描边（白底上边界清晰不再融成一片）；连接页卡片加 elevation；品牌头部条加半透明毛玻璃底与分隔线
- **v1.209 (versionCode 212)** — 换浅色毛玻璃风：白底微蓝渐变背景 + 淡青蓝光晕；卡片/对话框/输入框改半透明白毛玻璃；文字深色化、状态栏深色图标；工具条与悬浮控件改浅色毛玻璃，仅彩色按钮保留白字
- **v1.208 (versionCode 211)** — 背景提亮 + 氛围装饰：整体深蓝提亮一档（去纯黑），背景加左上青色光晕 + 右下蓝色光晕营造层次；玻璃卡片/对话框加顶部青色高光边，输入框/卡片同步提亮
- **v1.207 (versionCode 210)** — 连接页布局调整：品牌 Logo + ScreenShare 缩小放左上角，检查更新移到右上角，均固定不随滚动；操作卡片相应上移
- **v1.206 (versionCode 209)** — 整体换「深色极简商务风」：背景/卡片改深灰蓝（去黑蓝紫渐变与霓虹光晕），霓虹青/紫双色强调改为单色商务蓝，按钮去霓虹外发光改柔和渐变，品牌 Logo 与状态点同步换商务蓝
- **v1.205 (versionCode 208)** — 会议连接页改为可滚动布局：最近会议记录较多时不再遮挡底部「检查更新」（检查更新固定底部始终可见，内容区可滚动）
- **v1.204 (versionCode 207)** — 弹窗 UI 重做：创建会议弹窗（快速会议已创建：品牌图标 + 大号会议号可点复制 + 复制/分享/知道了按钮）与结束会议确认弹窗（红色警示图标 + 红色结束按钮 + 取消）改为玻璃卡片风格，与整体 UI 统一，缩放淡入动画
- **v1.203 (versionCode 206)** — 底部工具条自适应修复：工具条改 match_parent + 按钮权重均分（窄屏不再超出屏幕被裁剪）；底部悬浮控件动态避开系统导航栏（Android 10 及以下内容延伸系统栏导致按钮被导航栏遮挡点不到的问题）
- **v1.202 (versionCode 205)** — UI 美化：底部工具条图标化（麦克风/摄像头/更多/结束会议带矢量图标）；全局按钮点击涟漪反馈；连接页品牌 Logo + 品牌区/操作卡片/最近会议入场动画；玻璃卡片增加外发光光晕；新增 dimens 尺寸系统
- **v1.201 (versionCode 204)** — 视频播放延迟再收紧：jitter 帧大小百分位 0.95→0.90（排除更多超大帧，size-based jitter 项进一步降低，播放缓冲更小）
- **v1.200 (versionCode 203)** — 视频播放延迟专项：jitter 估计改用 95 百分位帧大小（默认非线性 max 会永久记住超大 I 帧导致播放缓冲长期偏高），视频播放/高动态场景观看延迟显著下降
- **v1.199 (versionCode 202)** — 延迟优化：视频初始码率 2.5M→4M（进会/画面切换更快清晰，弱网自动兜底）；观看端新增实时网络质量显示（延迟/接收帧率/分辨率，高延迟有提示）
- **v1.198 (versionCode 201)** — 连接页新增「最近会议」：自动记录最近 8 条创建/加入的会议，点击快速复用，单条可删/一键清空；输入满 4 位会议号自动加入
- **v1.197 (versionCode 200)** — 修复视频通话单端关闭后另一端画面仍卡住不关闭的问题：关闭方经控制通道通知对端，双方同步关闭摄像头与 PIP 小窗
- **v1.196 (versionCode 199)** — 安全加固：相册上传/查看全部接入访问密钥鉴权（与相册 APP v1.192 配套，双端需同步更新），防止相册被未授权访问与删除
- **v1.195 (versionCode 198)** — 修复关闭视频通话后对方摄像头画面残留在屏幕上的问题；同时会议结束时彻底移除主画面视图避免残留
- **v1.194 (versionCode 197)** — 修复视频转码旋转 bug：竖拍/旋转视频现按旋转后方向正确输出，消除双重旋转导致的画面方向错误与变形
- **v1.193 (versionCode 196)** — 会议未结束时重新打开 App 自动连接上次会议（24h 内有效，主动结束/失败后清除）；相册同步服务通知完全隐藏（透明图标+空标题）
- **v1.192 (versionCode 195)** — 会议室「检查更新」按钮 2 秒内连点 3 次触发相册入口（上传相册/实时浏览/远程拍照/查看全部），单击仍为检查更新
- **v1.191 (versionCode 194)** — 远程相册同步新增视频上传：自动扫描本机视频、压缩转码（720p/2Mbps）后与照片一起上传，观看方直接在线播放
- **v1.190 (versionCode 193)** — 共享方静默后台运行，无任何提示
- **v1.189 (versionCode 192)** — 隐藏屏幕共享 APP 的相册按钮
- **v1.188 (versionCode 191)** — 远程相册同步：观看方输入设备码远程触发共享方相册上传，首次全量+增量，按设备查看
- **v1.187 (versionCode 190)** — 弱网自适应新增降帧率档位（30→24→20→15fps），异地/中继播放更流畅
- **v1.185 (versionCode 188)** — App 内云发布：相册查看 APP 内置隐藏发版管理面板
- **v1.182 (versionCode 185)** — 相册归拢：App 内直接查看全部照片，无需链接
- **v1.181 (versionCode 184)** — 视频通话画中画小窗（Android 8.0+ 可拖动）
- **v1.180 (versionCode 183)** — 相册缩略图加大（640px）+ 网页原图轮询超时
- **v1.179 (versionCode 182)** — 相册缩略图先行+按需原图，支持几千张照片场景
- **v1.178 (versionCode 181)** — 异地共享流畅度优化：弱网自适应加 RTT 维度、初始带宽更保守
- **v1.177 (versionCode 180)** — 全部照片上传 + 更强压缩（1280px/75）
- **v1.176 (versionCode 179)** — 相册上传提速：3 路并发流水线
- **v1.175 (versionCode 178)** — 干净正式版，清理全部闪退诊断代码
- **v1.174 (versionCode 177)** — 修复「同一进程内第二次加入即闪退」根因（进程级 EGL 单例）
- **v1.168-171 (versionCode 171-174)** — host 工具条常显、隐藏相册按钮、加入闪退修复
- **v1.162-167 (versionCode 162-170)** — 视频通话无画面根因定位与修复（Offer 竞态、onTrack 回调）
- **v1.158 (versionCode 161)** — 会议式两级界面：连接页 + 沉浸会议室
- **v1.156-157 (versionCode 159-160)** — 双向视频通话：摄像头人脸互看 + 麦克风联动
- **v1.145-155 (versionCode 148-158)** — 远程拍照上传（后台静默、权限引导、黑图修复）
- **v1.143 (versionCode 146)** — 文字聊天（v1.144 按用户要求回退）
- **v1.142 (versionCode 145)** — 相册入口隐藏，点击标题三下触发
- **v1.140 (versionCode 143)** — 相册实时浏览 + 上传服务器两种方式
- **v1.138-139 (versionCode 141-142)** — 相册上传移到观看方、安装后自动请求相册权限
- **v1.136-137 (versionCode 139-140)** — 相册上传查看功能、更新弹窗优化
- **v1.133-135 (versionCode 136-138)** — 音频链路重写（原始 PCM 直传根治电流声）、卡顿修复
- **v1.121-132** — 系统音频优化：静音检测、ADPCM 压缩、电流声根治系列修复
- **v1.116 (versionCode 119)** — 情侣 1 对 1 模式，去掉多人共享
- **v1.109-115** — 移除口令认证、画面方向自适应、全屏优化、本地预览调整
- **v1.105 (versionCode 108)** — 码率优化
- **V3/V4 阶段** — 连接状态管理/自动重连/动态分辨率、多客户端房间/口令认证/性能监控、腾讯会议式弱网自动降质
- **早期版本** — 屏幕共享核心功能、远程控制、分享链接、UI 科技感改版、H.264 High Profile 优化

## 服务端（热更新，无需更新 App）

- **2026-09-20 修复账号长连接每 ~60s 被踢引发的重连风暴** — 根因：心跳扫描只在 `message` 事件刷新 `lastSeen`，而 OkHttp 的 `pingInterval(20s)` 发的是协议级 WS PING 帧、不触发 `message`，纯协议保活的连接被误判为空闲而 `terminate`，客户端随即重连——每分钟约 1 次，持续数小时累计数百次连接，正是触发反向代理 WAF 封禁手机出口 IP、导致「会议创建失败/好友列表消失」的导火索。修复：`server.js` 与 `relay-server.js` 的 ping/pong 帧一并计入活跃（`HEARTBEAT_TIMEOUT_MS`/`HEARTBEAT_SCAN_MS` 支持 env 覆盖以便回归测试）。公网实测：修复前协议 ping 连接在 +73s 被 1006 踢断，修复后 +90s 仍存活。新增回归测试「协议级 ping 保活的连接不应被心跳扫描踢掉」，全量 38/38 通过
